<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRE Results</title>
</head>
<body>
  <h1>Chemical Reaction Engineering Results</h1>
  <% if (result) { %>
    <p>The calculation results are:</p>
    <ul>
      <% for (const key in result) { %>
        <li><b><%= key %>:</b> <%= result[key] %></li>
      <% } %>
    </ul>
  <% } else { %>
    <p>An error occurred during the calculation. Please try again.</p>
  <% } %>
</body>
</html>
